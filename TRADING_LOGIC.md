# 매매 전략 설계 문서 — 의도와 매수·매도 로직 상세

이 문서는 `pages/1_백테스트.py`("유효성 검증" 페이지)와 `gold_dashboard/backtest.py`에 구현된
신호 기반 매매 전략을 **왜 이렇게 설계했는지**, 그리고 **정확히 어떤 조건으로 매수·매도가
일어나는지**를 정리한 문서입니다. 사용법 위주인 `README.md`와 달리, 이 문서는 설계 의도와
로직의 세부 규칙에 집중합니다. 11장은 이 금 상관관계 모델이 지표 선정부터 지금 형태에
이르기까지 실제로 거친 접근 방식과 결론 도출 과정을 별도로 정리한 것입니다.

---

## 1. 이 프로세스를 만든 의도

### 1-1. 출발점: "금과 상관관계가 있다고 알려진 지표들을, 실제로 믿을 수 있는가?"

대시보드(`app.py`)는 실질금리·달러인덱스·WTI·VIX 네 지표를 한 화면에 모아
보여주는 **참고용 표**로 시작했습니다. 그런데 "역상관 관계가 있다"는 학술적 주장(Erb &
Harvey 2013, World Gold Council 등)을 표로만 보여주는 것과, 그 관계를 **실제 매매 규칙으로
바꿨을 때 정말 돈이 되는지**는 전혀 다른 질문입니다. `pages/1_백테스트.py`("유효성 검증"
페이지)는 이 질문에 답하기 위해 별도로 분리된 페이지입니다 — 이름 그대로, 대시보드에 나오는
상관관계가 실제 매매 신호로서 **유효성이 있는지**를 검증하는 용도입니다.

이 둘의 역할을 명확히 분리한 것이 첫 번째 설계 의도입니다:

- **대시보드(`app.py`)**: 4개 지표의 현재 상태를 있는 그대로 보여주는 참고 화면. 어떤 설정을
  바꾸더라도(백테스트 페이지의 분석 기간, 금 가격 기준 등) 이 페이지의 표·그래프는 절대
  영향을 받지 않습니다 — 항상 국제 시세, 항상 고정된 기간입니다.
- **유효성 검증(`pages/1_백테스트.py`)**: 그 지표들을 실제 매매 규칙으로 조합했을 때의 성과를
  시뮬레이션하는 실험실. 여기서는 임계값·필터 ON/OFF 등을 자유롭게 바꿔가며 "이 규칙이
  실제로 Buy & Hold를 이기는가"를 확인할 수 있습니다.

### 1-2. 왜 "매매 규칙"인가 — 단순 상관관계와 트레이딩 신호의 차이

대시보드의 "핵심 요약"에 정리했듯, 실질금리·달러인덱스는 **구간에 따라 상관성의 강도가 크게
달라집니다**(2005~2026 11개 구간 중 실질금리·달러인덱스가 동시에 약했던 구간은 2021~2022
단 한 번). 즉 어느 한 지표만 보고 매매하면 특정 국면에서 신호가 무력화될 위험이 있습니다.
그래서 이 전략은 처음부터 **단일 지표가 아니라 여러 신호를 OR로 결합**하는 구조로
설계했습니다:

- 실질금리+달러인덱스의 이동평균 돌파 조합(`green_count`) — 정책 개입기·구조적 변화에 강함
- 52주 신고가/신저가 갱신 — 가격 자체가 만들어내는 모멘텀 신호, 위 조건과 완전히 독립적

하나가 잘 안 통하는 구간에서도 다른 하나가 대신 신호를 내주기를 기대하는 구조입니다. 다만
이 설계의 명확한 한계도 대시보드 "핵심 요약"에 그대로 적어뒀습니다 — 2021년 이후 각국
중앙은행(특히 신흥국)의 금 매입이 실질금리·달러인덱스의 설명력 자체를 흔들고 있고, 이
중앙은행 매입은 분기 단위로만 공개되어 이 시스템이 실시간으로 반영할 수 없습니다.

### 1-3. 매도 반응성과 "노이즈에 휘둘리지 않기"는 서로 다른 장치로 분리

코드 전반에 반복되는 설계 원칙 하나 — **매도 신호에 얼마나 빨리 반응할지(반응성)와, 대세
상승장 중의 일시적 노이즈에 흔들리지 않을지(내구성)는 서로 다른, 독립적인 두 장치가 각각
담당한다**는 점입니다:

1. **매수 후 최소 보유일수** (`min_holding_days`, 기본값 **0일** = 제한 없음): 매수 후 이
   일수가 지나기 전까지는 매도 조건 자체를 평가하지 않는 장치입니다. 기본값이 0이라
   기본적으로는 매도 신호(green_count·52주 신저가 갱신, 그리고 노이즈 필터를 거친 결과
   포함)가 뜨면 보유 기간과 무관하게 매수 다음 날이라도 즉시 매도할 수 있습니다 — 더 긴
   최소 보유 기간을 둔 전략을 시뮬레이션하고 싶을 때 사용자가 직접 올려서 쓰는 옵션입니다.
2. **상승추세 중 매도신호 노이즈 필터**: 아래 4장에서 자세히 다루는, 이 장치와는 완전히
   별개의 로직으로 대세 상승장 한복판에서 뜨는 일시적인 매도 신호에 바로 반응해 포지션을
   던지지 않도록 막는 장치입니다. 기본값 ON이며, `min_holding_days` 설정과 무관하게 항상
   독립적으로 작동합니다.

노이즈 필터는 "신호가 뜨면 즉시 반응한다"는 기계적 규칙이 실제로는 **단기 노이즈에 휘둘려
장기 상승 추세의 대부분을 놓치는 결과**로 이어질 수 있다는 문제의식에서 나왔습니다 (아래
9장 참고).

### 1-4. "국제 시세"만으로는 부족하다 — KRX 금현물을 실제 투자 기준으로 추가

이 백테스트는 처음엔 GC=F(국제 선물) 가격만 사용했습니다. 하지만 GC=F는 국내 투자자가 실제로
사고팔 수 있는 상품이 아니라 참고 가격에 가깝습니다. 그래서 **KRX 금현물시장(종목코드
04020000)의 실제 국내 원화 시세**를 두 번째 기준으로 추가했습니다(② 옵션, 현재 기본값) —
환율을 곱한 환산값이 아니라 실제 거래되는 시세 그 자체입니다. 여기에 실제 매매 시 발생하는
거래수수료(매수/매도 각 기본 0.165%)와 실물 보유 중 발생하는 보관수수료(기본 0.00022%/일)
까지 반영해서, "이 전략을 실제로 국내에서 실행했다면 얼마를 벌었을까"에 최대한 가깝게
시뮬레이션하는 것이 의도입니다(6-2절 참고).

---

## 2. 전체 데이터 흐름

```
① 원시 데이터 수집 (fetch_raw_data → timeseries.fetch_backtest_frame)
   real_rate(FRED DFII10), dxy(Yahoo DX-Y.NYB),
   gold(① GC=F 또는 ② KRX 04020000 — 선택된 기준)
        ↓
② 신호 계산 (compute_signals)
   real_rate/dxy 각각의 7/30/90일(역일) 이평선 대비 "금값에 우호적인가" 판정 → green_count(0~6)
   gold_sma_long = gold의 180일(역일) 이평선 (매도·매수 노이즈 필터가 공유)
   gold_new_52w_high / gold_new_52w_low = gold가 직전 365일(역일) 최고가/최저가를
   오늘 처음 넘어섰는가/내려갔는가
        ↓
③ 분석 기간으로 자르기 (trim_to_backtest_window)
        ↓
④ 매매 상태 머신 실행 (run_backtest) — 날짜별로 순회하며 아래 3·4장의 규칙 적용
        ↓
⑤ 성과 지표 계산 (compute_metrics, compute_hybrid_cagr, yearly_returns)
```

`prepare_signals()`가 ①~③(네트워크 의존적이고 느린 부분)를, `simulate()`가 ④~⑤(순수
계산)을 맡도록 나눠서, 매수·매도 임계값처럼 데이터 재수집이 필요 없는 설정을 바꿀 때는
①~③을 다시 하지 않고 캐시된 `signals` 위에서 `simulate()`만 재실행합니다.

### 2-1. ① 원시 데이터 수집의 미국/한국 시차 정합 (② KRX 기준 전용, 2026-09-16 수정)

FRED(`real_rate`)·Yahoo(`dxy`)는 관측치를 **미국 거래일** 날짜로 라벨링합니다(그 거래일의
세션이 미국 동부시간 오후 4~5시경 마감 — 한국시간으로는 그 다음 날 새벽 6~7시경). 반면
② KRX 금현물(`gold`)은 **한국 거래일** 날짜로 라벨링되고, 그 세션은 오후 3:30 KST에 이미
끝나 있습니다. 따라서 "같은 날짜 라벨끼리 그대로 합친다"고 하면, 아직 확정되지 않은(다음
날 새벽에야 나오는) 미국 데이터를 그보다 몇 시간 앞서 이미 마감된 그날 KRX 금 종가와 짝짓는
셈이 되어 **하루치 look-ahead bias**가 생깁니다(제3자 코드 리뷰로 2026-09-16 확인).

`timeseries.fetch_backtest_frame()`은 ② KRX 기준일 때만 `real_rate`/`dxy`의 날짜 인덱스에
`+timedelta(days=1)`을 더한 뒤 기존과 동일하게 outer-join + `ffill()`을 적용해 이를
바로잡습니다 — 미국 금요일 데이터가 라벨만 토요일로 밀리고, 그 뒤에 오는 다음 첫 한국
거래일(보통 월요일)까지 `ffill()`이 자동으로 그 값을 이어 붙여주므로, 별도의 거래일 캘린더
매칭 로직 없이 정확한 정렬이 됩니다. ① 국제 시세(GC=F) 기준일 때는 GC=F도 미국 거래일
라벨이라 real_rate/dxy와 이미 같은 달력을 쓰므로 이 보정이 필요 없고 적용되지도 않습니다.

이 수정으로 신호 발생 시점이 하루 뒤로 밀리므로, ② KRX 기준의 과거 백테스트 결과(매매
시점·매매 횟수·수익률 전부)가 이 수정 이전과 달라집니다 — 이전 수치는 look-ahead bias로
부풀려져 있었던 것이므로, 수정 후 수치가 더 신뢰할 수 있는 값입니다.

### 2-2. "당일 종가에 체결"이 실제로 가능한가 — KRX 마감 단일가매매 구조

이 문서 전반의 "즉시 체결"(신호 발생일 = 체결일, 그날의 종가로 체결)이라는 가정은 미국
지표와의 시차 문제(2-1절)와는 별개로, "장이 마감돼야 확정되는 그날 종가로 그날 바로
체결할 수 있는가"라는 별도의 질문을 남깁니다. KRX 금시장은 15:20~15:30(마감 10분)에
**마감 단일가매매(동시호가)** 방식으로 종가를 결정하며(KOSPI/KOSDAQ과 동일한 임의종료
메커니즘도 적용됨), 이 구간에서는 예상체결가 정보를 보면서 참여할 수 있습니다. 따라서
이 가정은 **KRX 마감 단일가매매 구조상 근사적으로는 실현 가능**하나, 임의종료 때문에 정확한
체결 시각·최종가가 몇 분 전에 100% 확정되는 것은 아니어서 **완전히 동일한 가격이 보장되지는
않는 시장 미시구조상의 한계**가 있습니다(제3자 코드 리뷰로 2026-09-16 확인, KRX 금시장
매매거래시간 규정 참고). 신호일 종가 대신 익일 시가(next-day open) 체결로 전환하는 옵션은
검토했으나 우선순위가 낮아 보류했습니다.

---

## 3. 매수 로직 상세

**미보유 상태일 때만** 아래 두 조건을 매일 평가합니다. 둘 중 하나라도 만족하면 매수합니다
(OR 조건). 모든 조건은 지연 없이 신호 당일 종가에 즉시 체결됩니다 — 과거에 있던 지연 체결
기능(`entry_delay_days`)은 완전히 제거되었습니다(9장 설계 이력 참고). 코드 위치:
`run_backtest()`의 `if not holding:` 블록.

### 3-1. green_count 조건 — "실질금리·달러인덱스가 폭넓게 금에 우호적인가"

- **조건**: `green_count >= buy_green_count` (기본값 6, 즉 만점)
- **green_count란**: 실질금리·달러인덱스 두 지표 각각에 대해 7일/30일/90일(역일) 이동평균과
  비교해 "금값에 우호적인 쪽"에 있으면 1점씩, 총 6개 셀 중 몇 개가 우호적인지 합산한 값
  (0~6). 두 지표 모두 **역상관**이므로(`config.CORRELATION_DIRECTION`), "종가가 이동평균
  **이하**"일 때 우호적으로 판정합니다(`signals.gold_friendly_vs_ma`) — 금리·달러가
  오르는 추세가 아니라 오히려 눌려 있는 상태가 금에 유리하다는 논리입니다.
- **체결 방식**: **즉시 체결**. 지연 없이 신호 당일 종가에 바로 체결됩니다.
- **왜 만점(6)이 기본값인가**: green_count는 "두 지표가 부분적으로만 우호적인" 애매한
  상태를 배제하고, 두 핵심 매크로 변수가 **동시에, 모든 시간대(7/30/90일, 역일)에서** 금에
  우호적인, 확신도가 가장 높은 순간에만 진입하기 위한 보수적 기준입니다.

### 3-2. 52주 신고가 갱신 조건 — "새 국면의 시작을 놓치지 않기"

고급 설정의 "52주 신고가 갱신 시 매수" 체크박스(기본값 **켜짐**)가 켜져 있을 때만 평가되며,
green_count 조건과는 **완전히 독립적**입니다(같이 켜져 있어도 서로 영향을 주지 않고 둘 다
각자의 조건대로 평가됨).

- **조건**: 금 종가가 직전 `FIFTY_TWO_WEEK_WINDOW_DAYS`(기본 365일, 역일) 동안의
  최고 종가보다 **오늘 처음으로** 높아진 날 (`gold_new_52w_high`, `compute_signals()`에서
  계산). "지금 52주 최고가 상태를 유지 중"이 아니라 "오늘 막 새로 경신한 날"만 잡는
  조건입니다(`shift(1)` 뒤 비교로 구현).
- **체결 방식**: **즉시 체결**. 지연 없이 신호 당일 종가에 바로 체결됩니다.
- **빈도 제한**: 없습니다. 이 트리거 자체의 쿨다운은 없어서, 미보유 상태에서 신고가를
  경신하는 날마다 매번 작동합니다(다만 매수 후에는 보유 중이므로 매수 트리거 자체가 평가되지
  않습니다 — 실질적인 "재매수 빈도"는 얼마나 자주 매도 후 다시 신고가를 경신하느냐에 달려
  있습니다).
- **과거 "신고가 경신" 트리거와의 차이**: 이 프로젝트에는 예전에 "금의 신고가 경신" 트리거가
  있었다가(9장 설계 이력 참고) 한때 재진입 로직(장기추세 필터+단기 재돌파 조합)으로 대체된
  적이 있습니다 — 그 옛 트리거는 금의 **전체 역사상 최고가(all-time high)**를 기준으로 매수
  조건 자체를 대체하는 방식이었고, 강세장 중반처럼 이미 한동안 신고가를 경신해 온 국면에서는
  잘 작동하지 않는다는 문제가 있었습니다. 그 재진입 로직도 이후 2026-09-15에 완전히
  제거되었습니다(9장 참고). 이번 52주 신고가 조건은 그와 별개로, ① 기준을 **52주(365일)
  최고가**로 좁히고 ②
  다른 매수 조건을 대체하지 않고 그 위에 추가되는 독립적인 OR 조건 하나로 설계해 같은 문제를
  피했습니다.

### 3-3. 매수 조건 요약

| 조건 | 임계값(기본) | 체결 | 빈도 제한 |
|---|---|---|---|
| green_count | ≥ 6 | 즉시 체결 | 없음 |
| 52주 신고가 갱신 | 직전 365일 최고 종가 초과 | 즉시 체결 | 없음 |

위 두 조건 중 무엇으로 발생한 매수 신호든, "체결"이 실제로는 즉시가 아니라 5장의
**하락추세 중 매수신호 노이즈 필터**를 한 번 더 거칠 수 있습니다(기본값 ON) — 이 필터가
개입하면 신호 발생일이 아니라 확인이 끝난 날 매수됩니다. 5-4절 참고.

---

## 4. 매도 로직 상세

**보유 상태일 때만** 평가합니다. 코드 위치: `run_backtest()`의 `else:` 블록.

### 4-1. 최소 보유일수 — 모든 매도 평가의 선행 조건 (기본값 0일 = 비활성)

`min_holding_days`(기본값 **0일**, 역일/달력일 기준 — 주말·공휴일 관계없이 매수일로부터
`date2 - date1`로 계산)가 지나기 전까지는 **아래 4-2·4-3 조건을 아예 평가하지 않습니다**.
기본값이 0이므로 기본적으로는 이 조건이 매도를 전혀 막지 않습니다 — 매수 다음 날이라도
매도 신호가 뜨면 즉시 매도됩니다. 더 긴 최소 보유 기간을 둔 전략을 시뮬레이션하고 싶을 때
이 값을 직접 올려서 쓰는 옵션입니다(1-3절 참고). 5장의 매도신호 노이즈 필터는 이 값과
무관하게 항상 독립적으로 작동합니다.

### 4-2. green_count 조건 — "매크로 환경이 더 이상 금에 우호적이지 않다"

- **조건**: `green_count <= sell_green_count` (기본값 0, 즉 6개 셀 전부 비우호)
- **체결 방식**: **즉시 체결**. 지연 없이 신호 당일 종가에 바로 체결됩니다.
- **왜 0(전부 비우호)이 기본값인가**: 매도는 매수보다 훨씬 보수적으로 설계했습니다 —
  실질금리·달러인덱스 중 하나라도 아직 금에 우호적인 흔적이 남아 있으면 버티고, 완전히
  다 돌아섰을 때만 매도 신호로 인정합니다.

### 4-3. 52주 신저가 갱신 조건 — "새 하락 국면의 시작을 놓치지 않기"

고급 설정의 "52주 신저가 갱신 시 매도" 체크박스(기본값 **켜짐**)가 켜져 있을 때만 평가되며,
3-2절의 52주 신고가 매수 트리거를 그대로 매도 쪽에 대칭시킨 조건입니다.

- **조건**: 금 종가가 직전 `FIFTY_TWO_WEEK_WINDOW_DAYS`(기본 365일, 역일) 동안의 최저
  종가보다 **오늘 처음으로** 낮아진 날(`gold_new_52w_low`, `compute_signals()`에서 계산).
  "지금 52주 최저가 상태를 유지 중"이 아니라 "오늘 막 새로 경신한 날"만 잡는 조건입니다.
- **체결 방식**: **즉시 체결**. 지연 없이 신호 당일 종가에 바로 체결됩니다.
- **빈도 제한**: 없습니다. 보유 중 신저가를 경신하는 날마다 매번 작동합니다.

### 4-4. 매도 조건 요약

| 조건 | 임계값(기본) | 체결 | 선행조건 |
|---|---|---|---|
| green_count | ≤ 0 | 즉시 체결 | 최소 보유일수(기본 0일 = 없음) 경과 |
| 52주 신저가 갱신 | 직전 365일 최저 종가 미만 | 즉시 체결 | 최소 보유일수(기본 0일 = 없음) 경과 |

두 조건 모두 만족해서 "매도 신호가 오늘 발생"으로 판정된 뒤에도, 아래 5장의 노이즈 필터를
한 번 더 거칩니다.

---

## 5. 매도/매수신호 노이즈 필터

매도·매수 신호 실행 직전에 각각 개입하는 독립적인 두 장치입니다 — 먼저 원조 격인 매도
쪽(`use_sell_noise_filter`, 기본 ON)을 5-1~5-3절에서
설명하고, 그 뒤 완전히 대칭시킨 매수 쪽(`use_buy_noise_filter`, 기본 ON)을 5-4절에서
설명합니다. green_count·52주 신저가 매도 신호가 "오늘 체결"로 확정된 시점에 매도 쪽 필터가
끼어들어, 아래 규칙으로 실제 체결 여부를 다시 판정합니다.

### 5-1. 발동 조건 (게이트)

매도 신호가 뜬 날(D0)의 금 종가가 180일 이동평균보다 `sell_noise_filter_buffer_pct`%
(기본값 5%) **이상 높을 때만** 이 필터가 개입합니다. 그 미만이면 필터가 아예 작동하지
않고 신호 즉시 그대로 매도합니다 — 이미 추세가 꺾였거나 약한 구간에서는 노이즈 필터가
필요 없다는 판단입니다.

### 5-2. 관찰모드 진입과 판정 (핵심 로직)

필터가 발동하면:

1. **D0의 매도 신호는 즉시 실행되지 않고 무시됩니다.** 대신 D0의 날짜와 종가를 기록하고
   "관찰모드"에 들어갑니다. 관찰 중 추가로 뜨는 매도 신호는 매도 여부에 전혀 영향을 주지
   않으며, `sell_noise_log`에 `occurrence_count`(D0 포함 관찰 중 신호 발생 총 횟수)로만
   참고 기록됩니다.
2. **매도 실행 여부를 확인하는 방식**은 `use_daily_band_confirmation`(UI 체크박스 "매도
   확인 - 매일 갱신 2시그마 밴드", 기본값 ON)으로 전환합니다. 한 관찰(에피소드)이 열릴 때
   결정된 방식은 그 에피소드가 끝날 때까지 유지됩니다(도중에 설정을 바꿔도 이미 열린
   관찰에는 영향 없음).

#### 방식 ① 매일 갱신 2시그마 밴드 (기본값, `use_daily_band_confirmation=True`)

D0 이후 1거래일차부터 `NOISE_BAND_MIN_CHECK_TRADING_DAYS - 1`(기본 1~7)거래일차까지는
**하락폭과 무관하게 무조건 보류**합니다 — 며칠 사이의 등락은 순수한 랜덤워크 노이즈일 확률이
높아서, 그 정도 기간만으로는 가격 확인 자체를 하지 않습니다. `NOISE_BAND_MIN_CHECK_TRADING_DAYS`
(기본 8)거래일차부터 `NOISE_BAND_MAX_TRADING_DAYS`(기본 21, 약 3주)거래일차까지는
**매일**, 그날 고유의 확인 밴드를 계산해 가격을 확인합니다.

**밴드 계산 (√t 법칙, 2시그마 근거):**

```
일간표준편차 = NOISE_BAND_MONTHLY_VOL_PCT / √NOISE_BAND_TRADING_DAYS_PER_MONTH
            = 4.9% / √21 ≈ 1.0694%
그날의 밴드(%) = NOISE_BAND_SIGMA_MULTIPLIER × 일간표준편차 × √(경과 거래일수)
            = 2 × 1.0694% × √t
```

`NOISE_BAND_MONTHLY_VOL_PCT`(기본 4.9%)는 금의 약 30년 역사적 월간 변동성입니다.
이를 `√21`(한 달 평균 거래일수)로 나눠 일간표준편차로 환산한 뒤, 무작위보행(random walk)의
누적 변동성이 경과 기간의 제곱근에 비례해 커진다는 √t 법칙에 따라 경과 거래일수의 제곱근을
곱하고, 2시그마(σ×2, 수익률이 정규분포를 따른다면 약 95% 신뢰구간에 해당)를 적용합니다.
다만 이는 "월 변동성 4.9%·정규분포"라는 단순화된 가정 위의 근사치이며, 금 수익률의 실제
분포나 최근 변동성 수준을 통계적으로 검증해 도출한 값은 아닙니다. 실제 확인이 시작되는
8일차부터의 대표값:

| 경과 거래일수(t) | 1(보류) | 5(보류) | 8 | 10 | 14 | 21 |
|---|---|---|---|---|---|---|
| 밴드(%) | 2.14 | 4.78 | 6.05 | 6.76 | 8.00 | 9.80 |

(공식 자체는 t=1부터 정의되지만 1~7일차는 확인을 하지 않으므로 실제로 쓰이는 건 8일차부터입니다.
21일차의 밴드가 정확히 `2 × 4.9% = 9.8%`인 것은 우연이 아니라, `√21`로 나눴다가 `√21`을
다시 곱하는 21일차에서 서로 상쇄되기 때문입니다.)

**판정:** 8거래일차부터, 그날 종가가 `D0 종가 × (1 − 그날의 밴드)` 이하로 떨어진 **첫날**
즉시 매도를 실행합니다. `NOISE_BAND_MAX_TRADING_DAYS`(21거래일) 동안 단 하루도 그날의
밴드에 도달하지 못하면 관찰을 종료하고 D0의 신호는 없었던 일처럼 처리합니다(이후 새 매도
신호부터 다시 1단계부터 시작).

이렇게 설계한 이유: 1~7일차는 아무리 크게 빠져도(예: 하루 만에 -3~4%) 무조건 무시합니다 —
이 정도 기간의 등락은 실제 추세 전환인지 순수 노이즈인지 구분할 근거가 부족하다고 보기
때문입니다. 8일차 이후부터는 날이 갈수록 밴드가 넓어지므로, 몇 주에 걸쳐 완만하게 빠지는
하락(예: 매주 ~4%씩)은 그만큼 넓어진 밴드를 결국 스스로 뚫고 지나가는 시점에 매도가
확정됩니다 — 추세 자체가 실제로 꺾인 완만한 하락은 (고정 7일 방식과 달리) 끝내 걸러내지
못하고 놓치는 일이 없도록 설계했습니다.

#### 방식 ② 고정 D0+7일 (레거시, `use_daily_band_confirmation=False`)

1. D0 + 7일(역일 기준, `SELL_NOISE_FILTER_WINDOW_DAYS = 7`) 시점을 확인일로 예약합니다
   (그 날짜가 주말·휴장일이면 그 이후 첫 거래일).
2. **확인일(D0+7)에, 그날 종가만으로 매도 여부를 결정합니다.** 판정 기준은 오직 하나:
   `확인일 종가 <= D0 종가 × (1 − sell_noise_filter_drop_pct / 100)` (기본값 5%, 즉
   D0 종가 대비 5% 이상 하락했는지) — 만족하면 확인일에 매도를 실행하고, 만족하지
   않으면(작은 되돌림·보합·상승 포함) 관찰모드를 **해제**합니다.

두 방식 모두, 이 필터로 실제 매도가 체결된 거래는 거래 내역의 매도 사유에
`(노이즈필터: D0=... 종가 ... 대비 N거래일차 종가 ..., N.N% 하락[N.NN%↓ 밴드 도달] → 매도)`
(방식 ①) 또는 `(노이즈필터: D0=... 종가 ... 대비 D0+7일 종가 ..., N.N% 하락[5%↓ 조건 충족]
→ 매도)`(방식 ②)가 덧붙어 표시됩니다.

### 5-3. 왜 이렇게 설계했는가

이 로직은 처음에 훨씬 복잡한 다중 규칙(연속 발생·창 내 누적 횟수 등을 모두 실행 조건에
반영)으로 구현했다가, 실제 사용자 요청으로 **완전히 재설계**됐습니다. 그 뒤 다시 한번,
"고정 7일·고정 5%" 단일 체크포인트를 "매일 갱신되는 2시그마 밴드"로 재설계했습니다:

- **"대세 상승장에서 살짝 빠진 걸로 조기 매도해서, 그 뒤의 큰 상승을 놓치는 것을 막는다"**
  가 여전히 유일한 목적입니다. 신호가 몇 번 더 떴는지 같은 부가 정보는 실행 로직에서 완전히
  분리해 참고용으로만 남깁니다 — 여러 규칙이 뒤섞이면 "왜 이번엔 팔았고 저번엔 안 팔았는지"를
  사람이 검증하기 어려워지기 때문입니다.
- 고정 7일·고정 5% 방식의 한계는, 하락 속도와 무관하게 항상 같은 기준(7일 뒤, 5%)만 보기
  때문에 **완만하지만 꾸준한 하락**(예: 매주 ~4%씩 여러 주에 걸쳐 빠지는 패턴)을 "아직 5%가
  안 됐다"며 계속 관찰모드를 해제해 결국 놓쳐 버릴 수 있다는 점이었습니다. 매일 갱신되는
  2시그마 밴드는 경과 기간이 길어질수록 확인 기준도 함께 넓어지되, 무작위보행의 √t 법칙에서
  따온 속도로 넓어져 "느리지만 진짜인 하락"과 "일시적 되돌림"을 고정 방식보다는 낫게
  구분합니다 — 다만 이 밴드는 **금의 30년 평균 월간 변동성 4.9%를 가정한 근사치**이며,
  실제 금 수익률 분포(정규분포보다 두꺼운 꼬리를 가질 수 있음)에 맞춰 통계적으로 엄밀히
  검증된 필터는 아닙니다. 최근 변동성 구간이 이 가정과 다르면 밴드가 너무 좁거나 넓을 수
  있습니다.
- `NOISE_BAND_MONTHLY_VOL_PCT`(4.9%)는 금의 장기(약 30년) 평균 월간 변동성을 사용한
  값으로, 최근 몇 년의 변동성이 이보다 높거나 낮으면 실제로 적절한 밴드 폭이 달라질 수
  있습니다 — 값을 조정하고 싶다면 이 상수만 바꾸면 됩니다(파생 공식은 자동으로 재계산됨).
- 이 필터를 끄면(`use_sell_noise_filter=False`) 이 로직이 도입되기 이전과 완전히 동일하게,
  매도 신호가 뜨는 즉시 항상 매도합니다 — 필터 유무의 효과를 그대로 A/B 비교할 수 있도록
  설계했습니다. 확인 방식 자체도(`use_daily_band_confirmation`) 마찬가지로 켜고 끌 수
  있습니다.

### 5-4. 대칭 미러 — 하락추세 중 매수신호 노이즈 필터 (`use_buy_noise_filter`)

매수 쪽에도 위 매도신호 노이즈 필터와 **완전히 동일한 메커니즘**을 방향만 반대로 적용합니다
(2026-09-15 추가). "대세 상승장에서 살짝 빠진 걸로 조기 매도하지 말자"의 매수판 대칭
문제의식 — "대세 하락장에서 살짝 튀어 오른 걸(데드캣 바운스) 진짜 반등으로 오인해 성급하게
매수하지 말자" — 에서 나왔습니다.

- **발동 조건(게이트)**: green_count·52주 신고가 갱신 중 무엇이든 매수신호가 뜬 날
  (D0)의 금 종가가 180일 이동평균보다 `buy_noise_filter_buffer_pct`%(기본값 5%,
  `DEFAULT_BUY_NOISE_FILTER_BUFFER_PCT`) **이상 낮을 때만** 이 필터가 개입합니다. 그
  이상(즉 SMA에 더 가깝거나 위)이면 필터가 아예 작동하지 않고 신호 즉시 그대로 매수합니다.
- **관찰모드와 판정**: 발동하면 D0의 매수 신호는 즉시 실행되지 않고 D0의 날짜·종가만 기록한
  뒤 관찰모드에 들어갑니다. 관찰 중 추가로 뜨는 매수 신호는 매수 여부에 전혀 영향을 주지
  않으며 `buy_noise_log`에 `occurrence_count`로만 참고 기록됩니다. 확인 방식은
  `use_buy_daily_band_confirmation`(UI 체크박스 "매수 확인 - 매일 갱신 2시그마 밴드", 기본값
  ON)으로 전환합니다 — 매도 쪽과 정확히 같은 `noise_band_pct()` 함수·같은 상수
  (`NOISE_BAND_MIN_CHECK_TRADING_DAYS`=8, `NOISE_BAND_MAX_TRADING_DAYS`=21,
  `NOISE_BAND_SIGMA_MULTIPLIER`=2, `NOISE_BAND_MONTHLY_VOL_PCT`=4.9%)를 사용하되, 매도
  쪽이 "그날의 밴드만큼 **하락**한 첫날 매도"인 것과 반대로 "그날의 밴드만큼 **상승**한 첫날
  매수"합니다(방식 ①, 8거래일차 +6.05% ~ 21거래일차 +9.80%). 방식 ②(`use_buy_daily_band_
  confirmation=False`)는 D0+`BUY_NOISE_FILTER_WINDOW_DAYS`(기본 7)일 고정 시점에 D0 종가
  대비 `buy_noise_filter_rise_pct`%(기본 5%, `DEFAULT_BUY_NOISE_FILTER_RISE_PCT`) 이상
  올랐는지만 확인합니다. 21거래일(방식 ①) 또는 D0+7일(방식 ②) 동안 확인 조건에 도달하지
  못하면 관찰을 종료하고 D0의 신호는 없었던 것으로 처리합니다(다음 매수신호부터 처음부터
  다시 시작).
- 이 필터로 매수가 실행된 거래는 거래 내역의 매수 사유에
  `(매수노이즈필터: D0=... 종가 ... 대비 N거래일차 종가 ..., N.N% 상승[N.NN%↑ 밴드 도달] →
  매수)`(방식 ①) 또는 `(매수노이즈필터: D0=... 종가 ... 대비 D0+7일 종가 ..., N.N% 상승
  [5%↑ 조건 충족] → 매수)`(방식 ②)가 덧붙어 표시됩니다.
- 매도 쪽과 마찬가지로 이 필터를 끄면(`use_buy_noise_filter=False`) 이 로직이 없던 것처럼
  매수 신호가 뜨는 즉시 항상 매수합니다.
- 이 필터가 적용된 이후의 매수 신호를 기준으로 국면별 참여율(대시보드 "핵심 요약"·유효성
  검증 페이지의 참여율 카드)과 수수료 반영 순수익률이 다시 계산됩니다 — 매수 시점 자체가
  이 필터로 바뀌므로, `holding_curve`를 입력으로 쓰는 모든 다운스트림 계산
  (`compute_metrics`, `compute_hybrid_cagr`, `regime.participation_rates`)이 자동으로
  반영합니다. 대시보드 "핵심 요약"이 참조하는 `regime.default_backtest_kwargs()`에도 이
  필터의 기본값(ON)이 포함되어 있습니다.

---

## 6. 보조 로직

### 6-1. 최소 보유일수 (`min_holding_days`, 기본 0일 = 없음)

4-1절 참고.

### 6-2. KRX 금현물 수수료 (`buy_fee_pct`/`sell_fee_pct`/`daily_holding_fee_pct`)

② KRX 금현물 기준을 선택했을 때만 적용됩니다(① 국제 시세는 실물이 아닌 참고 가격이므로
수수료 개념이 없음). 실제 수수료 구조에 맞춰 두 종류로 분리되어 있습니다 — 예전에는 이
둘을 "연간 보유수수료(`gold_holding_fee_annual_pct`)" 하나로 뭉뚱그려 매매 횟수와 무관하게
보유기간에만 비례해 차감했는데, 이는 실제 수수료 구조(매매할 때마다 내는 거래수수료 +
보유 잔량에 매일 붙는 보관수수료)와 달랐다(2026-09-14 수정).

- **매수/매도 수수료** (`buy_fee_pct`/`sell_fee_pct`, 각 기본 0.165%, 편도): 매수·매도가
  체결되는 그 시점에 거래대금 기준으로 1회성 차감됩니다.
  ```
  # 매수 시(run_backtest): equity_at_entry = running_equity * (1 - buy_fee_pct/100)
  # 매도 시: running_equity = equity_at_entry * (exit_price/entry_price)
  #          * holding_fee_factor * (1 - sell_fee_pct/100)
  ```
  보유기간과 무관하게 매매 횟수에 비례해 총액이 늘어나므로, 신호강도(6/0·5/1·4/2)마다
  매매 횟수가 다르면 거래수수료 총액도 그만큼 달라집니다. Buy & Hold는 분석기간 종료
  시점에 전량 매도한다고 가정해(2026-09-14 확인) 매도수수료를 마지막 날 1회만 반영합니다.
- **보관수수료** (`daily_holding_fee_pct`, 기본 0.00022%, **일률** — 연율이 아님): 보유
  잔량에 매일 부과되어 실제 보유 중인 날에만 누적됩니다.
  ```
  fee_factor = (1 − 수수료% / 100) ** 경과일수   # 일할 계산, 연 365일로 나누지 않음
  ```
  Buy & Hold(분석 기간 전체)와 신호전략의 실제 보유 구간 모두 이 방식으로 동일하게
  적용됩니다. 정확한 산정 주기(일할 계산 후 월초 청구 vs. 월말 잔량 기준 월 1회 부과)가
  아직 확인되지 않아 "일할 계산" 가정으로 구현되어 있습니다 — 실제 방식이 확인되면
  `DEFAULT_DAILY_HOLDING_FEE_PCT` 계수만 바꾸면 되도록 파라미터화해뒀습니다.

유효성 검증 페이지의 "수수료 반영" 체크박스로 위 세 값을 한 번에 0으로 두거나(끔) 원래
설정으로 복원할(켬) 수 있고, 누적수익률 차트에는 이 체크박스와는 독립적으로 수수료
반영("net")/미반영("총수익률", "gross") 곡선을 토글로 비교하는 체크박스가 별도로 있습니다
— 둘 다 `backtest.simulate()`를 매수·매도 수수료 0으로 한 번 더 호출해서 얻은 결과(trades/
holding_curve는 수수료와 무관하게 항상 동일)를 그대로 추가 트레이스로 얹어두고, 화면에서는
`Plotly.restyle`로 어느 세트를 보여줄지만 바꾼다(줌 상태를 잃지 않기 위해 Streamlit
rerun 없이 처리).

---

## 7. 금 가격 기준 (① 국제 시세 vs ② KRX 금현물)이 로직에 미치는 영향

`gold_price_basis` 설정(기본값 ② KRX)은 아래 표의 범위에 영향을 줍니다.

| 영향받는 것 | ① 국제 시세일 때 | ② KRX 금현물일 때 |
|---|---|---|
| green_count(실질금리·달러인덱스 판정) | 영향 없음 (애초에 금 가격과 무관) | 영향 없음 |
| 180일선(매도·매수 노이즈 필터가 공유), 52주 신고가/신저가 갱신 판정(365일), 매수·매도 체결가 | GC=F 기준 | KRX 04020000(KRW/g) 기준 |
| 거래/보관 수수료 | 적용 안 함(0%) | 적용함(매수/매도 각 기본 0.165%, 보관 기본 0.00022%/일) |
| 대시보드 메인 표·그래프 | 영향 없음 (항상 국제 고정) | 영향 없음 (항상 국제 고정) |

---

## 8. 성과 지표가 4개 그룹으로 나뉜 이유

같은 "수익률"이라도 무엇을 분모/분자로 쓰느냐에 따라 의미가 완전히 달라지기 때문에,
의도적으로 네 그룹으로 나눠 나란히 비교합니다:

1. **매매 개요**: 매매횟수·보유기간 비중·승률 — 전략이 실제로 얼마나 자주, 얼마나 오래
   포지션을 잡았는지에 대한 맥락 정보.
2. **Buy & Hold**: 분석 기간 내내 그냥 들고만 있었을 때의 벤치마크.
3. **신호전략(보유기간)**: 전략이 실제로 포지션을 들고 있던 기간에만 투자했다고 가정한
   순수 성과 — 미보유 기간(현금)은 전혀 반영하지 않습니다.
4. **신호전략(미보유기간 기대수익률 포함)**: 보유 기간엔 실제 금 수익률을, 미보유
   기간엔 사용자가 입력한 기대수익률(기본 연 10%, 예: 채권 대체투자 가정)을 이어붙인
   전체 기간 기준 성과.

③과 ④를 나란히 보면, "현금으로 그냥 놀리는 것"과 "다른 자산에 넣어두는 것"의 차이가
전체 성과에 얼마나 영향을 주는지, ①의 보유기간 비중과 함께 가늠할 수 있습니다. 기대수익률을
0%로 설정하면 ④는 정확히 ③과 일치하도록 검증되어 있습니다(귀속 방식: 매수일 당일은
무수익, 매도일 당일은 금 수익률 전부 반영 — `run_backtest()`가 실제로 체결하는 방식과
동일).

**③을 Buy & Hold의 CAGR과 곧바로 나란히 비교하지 마세요** — 분모(CAGR 계산의 기준 일수)가
다릅니다. ③은 "실제 보유했던 일수"만, Buy & Hold는 "분석 기간 전체"를 분모로 씁니다. ④는
분모가 분석 기간 전체라 Buy & Hold와 같지만, 여기에는 "미보유 기간엔 연 X% 가정"이라는
전제가 하나 더 얹혀 있으므로, ④가 ③이나 Buy & Hold보다 "더 보수적"이라거나 더 낫다고
서열을 매기지 말고, 서로 다른 질문에 답하는 별개의 수치로 참고하세요(2026-09-16: 이
비교를 위해 잠시 추가했던 "가정 없는 현금 0% 기준" 지표는 UI가 복잡해진다는 피드백에
따라 다시 제거했습니다).

---

## 9. 설계 이력 — 실제 거래 검증을 거쳐 바뀐 부분

이 로직들은 처음부터 이 형태로 만들어진 게 아니라, 실제 백테스트 결과에 나온 개별 거래를
하나하나 검증하는 과정에서 발견된 문제를 고치며 지금 형태로 발전했습니다. 앞으로 로직을
더 손볼 때 참고할 수 있도록 남겨둡니다.

- **`gold_short_ma_crossover_up` dtype 버그**: 재진입 트리거의 단기 이평선 상향 돌파 판정이
  `above_short & ~above_short.shift(1).fillna(False)`로 구현돼 있었는데, `.shift(1)`이
  선행 `NaN`을 만들면서 Series 전체가 `object` dtype으로 승격되고, `.fillna(False)`로도
  그게 되돌아오지 않아 `~`가 논리 NOT이 아니라 파이썬 정수에 대한 비트 NOT으로 동작하는
  버그였습니다(`~True == -2`, `~False == -1`, 둘 다 참으로 평가되어 사실상 "지금 그 이평선
  위에 있는가"와 같아져 버림 — "방금 막 돌파했는가"라는 원래 의도와 다름). 실제 KRX
  데이터로 검증한 결과(당시 `SHORT_REENTRY_WINDOW`는 20거래일이었음 — 이후 30일(역일)로
  변경) 버그 버전은 1,675일을 돌파로 잘못 판정했고, 고친 뒤에는 168일만 진짜 돌파로
  판정됐습니다. `.shift(1, fill_value=False)`로 수정해 dtype이 끝까지 bool로 유지되도록
  고쳤습니다(`gold_dashboard/backtest.py`).
- **모든 이동평균 창을 거래일 개수 기준에서 역일(달력일) 기준으로 전환**: 5·30·60거래일이던
  green_count 이평선을 7·30·90일(역일)로, 재진입 단기 이평선(20거래일→30일)·장기추세
  필터(200일→365일)·장기추세 기울기 비교 시차(20거래일 전→30일 전)를 모두 "일주일·한달·
  세달·1년" 표준 단위의 역일 기준으로 재정렬했습니다. `metrics.compute_sma`를 pandas의
  오프셋 기반 rolling(`"{N}D"`)으로 다시 구현하고, 장기추세 기울기 비교는 행 이동
  (`.shift(n)`) 대신 `metrics.value_n_days_ago`(달력일 기준 조회)로 교체했습니다. 최소
  보유일수·재진입 빈도 제한·매도 노이즈 필터의 관찰기간(7일)·확인 하락률(5%)은 애초부터
  역일 기준이었으므로 값 변경 없이 그대로 유지했습니다.
- **매도신호 노이즈 필터의 재설계**: 5-3절 참고. 처음엔 "연속 발생·누적 횟수"까지 실행
  조건에 섞은 복잡한 다중 규칙이었으나, "실행 여부는 순수 가격 비교 하나로만 결정하고
  나머지는 참고 정보로만 남긴다"는 지금 형태로 단순화됐습니다.
- **"몇 % 이상 빠져야 진짜 하락인가" 임계값 추가**: 노이즈 필터의 최초 버전은 D0+7일
  종가가 D0보다 "조금이라도" 낮으면 무조건 매도였습니다. 이는 상승장 중 자연스러운 등락
  에도 쉽게 매도로 이어져, 그 뒤에 이어지는 더 큰 상승분을 놓칠 위험이 있었습니다. 그래서
  "D0 대비 최소 몇 % 이상 하락해야 진짜 매도로 본다"는 `sell_noise_filter_drop_pct`
  (기본 5%) 임계값을 추가했습니다.
- **재진입 트리거로의 전환**: 재진입 조건은 과거 "금의 신고가 경신" 트리거를 대체한
  것입니다(모듈 docstring 참고) — 신고가 경신은 특정 국면(예: 이미 한동안 신고가를
  경신해 온 강세장의 중반)에서는 잘 작동하지 않는다는 판단에 따라, 지금의 "장기추세
  필터 + 단기 재돌파" 조합으로 교체했습니다.
- **52주 신고가/신저가 갱신 트리거 추가, 지연 체결 기능 완전 제거**: 매수 쪽에 독립적인
  "52주 신고가 갱신 시 매수" 트리거를 추가한 뒤, 이를 매도 쪽에 대칭시킨 "52주 신저가
  갱신 시 매도" 트리거도 추가했습니다(둘 다 자체 빈도 제한 없음, 기본값 ON). 이와 함께,
  green_count 신호에만 있던 매수·매도 지연 체결 기능(`entry_delay_days`/`exit_delay_days`
  및 그에 딸린 대기 주문 상태)을 완전히 제거해, 이제 모든 매수·매도 조건이 예외 없이
  신호 당일 종가에 즉시 체결됩니다.
- **장기추세 이동평균을 365일에서 180일로 단축**: 재진입 필터의 ① 장기추세 필터와 매도
  노이즈 필터의 관찰모드 진입 게이트는 원래 같은 `gold_sma_long`(당시 365일선)을 공유했는데,
  실거래 확인 과정에서 365일 평균은 후행성이 강해 — 예: 고점 대비 -19% 조정 중인데도 그
  1년 전 훨씬 낮았던 가격들이 평균에 남아 있어 여전히 "365일선+5%" 게이트를 통과하는 등 —
  "지금 확실히 강한 상승추세"라는 취지와 실제 판정이 어긋나는 사례가 확인되어
  `LONG_TREND_WINDOW`를 180일(6개월)로 단축했습니다. 52주 신고가/신저가 트리거의
  `FIFTY_TWO_WEEK_WINDOW_DAYS`(365일)는 이 변경과 무관한 별개의 상수로 그대로 유지됩니다.
- **"기준일" 입력 추가, `trim_to_backtest_window`의 종료일 클램프 누락 발견**: 유효성 검증
  페이지에 "기준일(오늘로부터 N년 전)" 입력을 추가하면서 `trim_to_backtest_window()`를
  들여다보니, 그동안 시작일(`end_date - years`)만 잘라내고 **종료일(`end_date`) 쪽은 아예
  자르지 않고** 있었습니다. 지금까지는 모든 호출이 `as_of`(즉 `end_date`)를 항상 "오늘"로
  넘겼고, 그 앞에서 데이터를 가져오는 `fetch_backtest_frame`도 항상 "오늘"까지만 받아왔기
  때문에 이 누락이 결과에 드러난 적이 없었습니다. 하지만 "기준일"을 과거로 설정하면
  `end_date`가 더 이상 데이터의 마지막 날짜와 같지 않게 되므로, 상한을 자르지 않으면 기준일
  이후의(미래) 데이터가 그대로 섞여 들어갈 수 있는 잠재적 버그였습니다. 방어적으로
  `df.index <= end_date` 조건을 추가해, `df`가 어떻게 만들어졌는지와 무관하게 항상 올바른
  구간만 남도록 고쳤습니다.
- **금/은비율을 대시보드에서 완전히 제거**: 금 가격과 유의미한 상관관계가 확인되지 않아,
  이미 백테스트 전략의 매수·매도 조건에서는 제외돼 있던(위 항목 참고) 금/은비율을 대시보드
  표·그래프·데이터 수집 로직에서도 전부 제거했습니다(`gold_dashboard/data_sources.py`의
  `fetch_gold_silver_ratio`, `gold_dashboard/signals.py`의 `ratio_threshold_active`
  포함). 이와 함께 유효성 검증 페이지의 국면 구간표·5단계 분류·신호강도별 참여율 계산을
  `gold_dashboard/regime.py`로 옮겨 대시보드 "핵심 요약"과 공유하는 단일 소스로 만들고,
  "핵심 요약"의 상승장/하락장 보유 비중 문구가 하드코딩된 숫자 대신 이 모듈의 실시간 계산
  결과를 그대로 바인딩하도록 바꿨습니다.
- **매도신호 노이즈 필터를 매수 쪽에도 대칭 적용(2026-09-15)**: 5-4절 참고. 기존 매도신호
  노이즈 필터의 √t 밴드 로직(`sell_noise_band_pct` 함수)을 방향에 무관한 `noise_band_pct`로
  일반화하고, 관련 상수도 `SELL_NOISE_BAND_*`에서 `NOISE_BAND_*`(양방향 공유)로 이름을
  바꿨습니다. 매도 전용이던 상태 머신(`sell_noise_state`/`sell_noise_log`)과 정확히 같은
  구조의 `buy_noise_state`/`buy_noise_log`를 매수 쪽에 병렬로 추가해, 하락추세(180일선
  −5% 이하) 중 발생하는 매수 신호도 동일한 확인 절차(2시그마 밴드 또는 D0+7일 고정)를
  거치도록 했습니다. 참여율·수수료 반영 순수익률 계산은 모두 `holding_curve`(매수·매도
  체결 시점 자체)로부터 파생되므로, 이 필터 적용 이후의 신호를 기준으로 자동 재계산됩니다.
- **재진입 로직(단기 재진입 트리거) 완전 제거(2026-09-15)**: 위 항목에서 보듯 재진입
  조건(① 180일선 버퍼+우상향 장기추세 필터, ② 30일선 상향 재돌파, 빈도 제한)은 실사용
  중 계속 기본값 OFF로 남아 있었고, 매수 신호는 green_count와 52주 신고가 갱신만으로도
  충분하다고 판단되어 기능 자체(`compute_reentry_trigger`, `gold_short_ma_crossover_up`
  컬럼, `SHORT_REENTRY_WINDOW`/`DEFAULT_LONG_TREND_BUFFER_PCT`/
  `LONG_TREND_SLOPE_LOOKBACK_DAYS`/`DEFAULT_REENTRY_FREQ_LIMIT_DAYS` 상수, `use_reentry_trigger`
  /`use_reentry_freq_limit`/`reentry_freq_limit_days`/`long_trend_buffer_pct` 파라미터,
  UI의 "단기 재진입 로직 사용" 체크박스 일체)을 완전히 삭제했습니다. `gold_sma_long`(180일
  이동평균, `LONG_TREND_WINDOW`)은 매도·매수 노이즈 필터가 여전히 공유해서 쓰므로 그대로
  유지됩니다.

---

## 10. 한계와 유의사항

- 과거 데이터 기반 시뮬레이션이며 미래 성과를 보장하지 않습니다.
- ② KRX 금현물 기준일 때는 매수·매도 거래수수료와 보관수수료가 반영되지만(6-2절 참고),
  세금과 슬리피지는 여전히 반영되지 않았습니다. 또한 신호 당일 종가 체결 가정 자체도 KRX
  마감 단일가매매 구조상 근사치입니다(2-2절 참고 — 완전히 동일한 가격이 보장되지는 않음).
- 표본 기간이 상대적으로 짧아(기본 10년, 1~15년 조정 가능) 과최적화(overfitting) 위험이
  있습니다. "기준일(오늘로부터 N년 전)"을 0보다 크게 설정하면 분석 종료일 자체를 과거로
  옮겨 특정 구간(예: 최근 급등기)을 일부러 제외하고 검증할 수도 있습니다.
- 실질금리·달러인덱스의 설명력은 구간에 따라 크게 달라지며, 특히 2021년 이후 각국
  중앙은행의 금 매입이라는, 이 시스템이 실시간으로 반영하지 못하는 변수의 영향을 받고
  있습니다(대시보드 "핵심 요약" 참고).
- "④ 신호전략(미보유기간 기대수익률 포함)"의 기대수익률은 사용자가 입력한 단일 연이율을
  그대로 연복리 적용한 단순 가정치이며, 실제 채권 등 대체 투자자산의 이자율 변동·재투자·
  신용위험은 반영돼 있지 않습니다.

---

## 11. 이 모델을 만든 과정 — 지표 선정부터 결론 도출까지

이 장은 "매매 로직이 왜 이 모양인지"가 아니라, **이 금 상관관계 모델이 지금 형태에 도달하기
까지 실제로 어떤 접근 방식을 거쳤는지, 그 과정에서 어떻게 결론을 도출했는지** 자체를
시간순으로 다룹니다.

### 11-1. 0단계 — 상관 지표 후보 선정과 유의성 검증 (모든 것의 출발점)

**"상관관계가 있다는 주장"을 전제로 깔지 않고, 검증 대상으로 삼는 것**이 첫 원칙입니다.
"실질금리·달러인덱스가 금과 역상관"이라는 건 Erb & Harvey(2013), World Gold Council 등
학술·업계에서 자주 인용되는 정설이지만, 이 프로젝트는 이를 그대로 믿지 않고 실제로
검증했습니다.

**절차:**

1. **후보를 넓게 수집한다.** 금 하나에도 실질금리·달러인덱스·WTI 유가·VIX·금/은비율까지
   다섯 개 후보를 놓고 시작했습니다 — "그럴듯하게 들리는 것"을 전부 일단 후보에 올리고,
   그중 일부만 최종 채택하는 방식입니다.
2. **전체기간 상관계수 하나로 판단하지 않는다.** 단일 상관계수는 특정 시기(큰 위기 하나)가
   전체 숫자를 왜곡할 수 있습니다. 대신 **분석 기간을 여러 개의 하위 구간으로 쪼개, 각
   구간마다 개별적으로 유의한 상관성이 있는지**를 봅니다. 금 프로젝트에서는 2005년 이후를
   11개 구간으로 나눠 검증했습니다.
3. **"일관되게 통하는가"를 신호팩터 채택의 기준으로 삼는다.** 구간별 견고성이 곧 판정
   기준입니다.

**금 프로젝트의 실제 결과:**

| 후보 지표 | 구간별 검증 결과 | 최종 처리 |
|---|---|---|
| 실질금리 | QE·팬데믹 등 정책개입기에 강한 상관(구간마다 강도는 다름) | **신호팩터 채택** (green_count 절반) |
| 달러인덱스 | 위기전이 국면·최근 구간에서 강한 상관 | **신호팩터 채택** (green_count 나머지 절반) |
| 실질금리+달러인덱스 **병행** | 11개 구간 중 **10개**에서 유의한 상관성 확인 | → 이 견고성이 두 지표를 묶어 신호팩터로 쓴 결정적 근거 |
| WTI 유가 | 인플레이션 헤지 채널로 정상관은 있으나 **단기 효과 중심·신뢰도 낮음** | 대시보드 참고용 표시만, 매매신호에서 제외 |
| VIX | 안전자산 수요와 약한 정상관이나 **최근 표본에서 효과 약화 추세** | 대시보드 참고용 표시만, 매매신호에서 제외 |
| 금/은비율 | 금 가격과 **유의미한 상관관계 자체가 확인 안 됨** | 매매조건 제외를 넘어 **대시보드·데이터수집 로직에서 완전 삭제**(9장 참고) |

핵심은 **"상관관계가 있다" ≠ "신호팩터로 쓴다"**라는 기준을 여기서 이미 세웠다는 점입니다.
WTI·VIX는 상관관계 자체는 있었지만 견고성 부족으로 참고용으로 강등됐고, 금/은비율은
유의성 자체가 없어 완전히 삭제됐습니다. 오직 여러 구간에 걸쳐 일관되게 유의했던 두
지표만 살아남아 실제 신호(green_count)가 됐습니다.

### 11-2. 그 이후 실전 검증에서 반복 확인된 교훈들

신호팩터를 확정한 뒤에도, 실제로 이 모델의 로직을 다듬고 검증하는 과정에서 같은
원칙("검증되지 않은 정교함은 과최적화일 뿐이다")이 다른 층위에서 계속 재확인됐습니다.
아래는 이 프로젝트가 실제로 거친 순서입니다.

1. **로직을 정교하게 다듬을수록 구현 버그의 여지도 커진다.** 노이즈 필터, look-ahead bias
   보정, 순/총수익률 분리 같은 장치를 추가·수정하는 과정에서 dtype 버그, 날짜 정합 오류 등
   실제 버그가 여러 번 발견됐습니다(9장 설계이력 참고). 장치 하나를 추가할 때마다 검증
   비용도 같이 늘어난다는 뜻입니다.
2. **지표·설정이 늘어날수록 해석이 흐려진다.** "CAGR(전체기간, 미보유0%)" 같은 참고 지표를
   추가했다가 "화면이 복잡해진다"는 이유로 다시 제거하는 등, 정보를 추가하는 것 자체가
   항상 이득은 아니라는 걸 UI 차원에서도 반복 경험했습니다.
3. **코드/데이터 버전이 살짝만 달라도 결과가 크게 달라질 수 있다.** 동일한 몬테카를로
   방법론(임의구간 표본, 승률 계산)을 "어제"와 "오늘" 두 번 돌렸는데, 두 결과가 완전히
   다르게 나온 적이 있습니다. 원인을 커밋 타임스탬프로 추적해보니 어제 실행은 매수
   노이즈필터·현재 수수료모델·look-ahead bias 수정이 반영되기 **이전** 코드였습니다 —
   즉 파라미터·필터 하나하나가 최종 수익률에 생각보다 훨씬 크게 기여한다는 걸 구체적
   수치로 확인한 사례입니다. 코드가 바뀔 때마다 이전 결과와의 비교표를 남겨 "무엇이 왜
   바뀌었는지" 추적 가능하게 해야 합니다.
4. **필터를 걷어내면 그 필터가 왜 있었는지 숫자로 드러난다.** 노이즈 필터·수수료를 기본값
   OFF로 바꾼 직후 "매도 시 인버스 ETF(연 0.45% 총보수)를 사면 어떻게 되는가"를
   테스트했더니, 직관과 반대로 그냥 현금 보유보다 **오히려 손해**(총수익률 254%→183%)였습니다.
   원인은 노이즈 필터가 꺼지며 매도 신호의 "진짜 하락 확신도"가 낮아졌고, 확신도 낮은
   신호에 레버리지성(인버스) 포지션을 얹으면 휘소(whipsaw)에 두 배로 당하기 때문입니다.
   → 필터를 하나 빼거나 더할 때는 반드시 그 필터가 실제로 무엇을 걸러내고 있었는지를
   A/B 테스트로 직접 확인해야 하며, "간단하게 만들기 위해 뺀다"는 결정에도 대가가
   따른다는 걸 인지해야 합니다.

### 11-3. 최종 결론 — 이 모든 과정에서 도출된 설계 철학

0단계부터 11-2절의 네 가지 사례까지 관통하는 패턴은 하나입니다: **장치(필터·파라미터)를
늘릴수록, 그게 실제로 미래에도 통할 진짜 구조적 신호인지 과거 데이터에만 맞춰진 우연인지
구분하기가 점점 어려워진다.** 이걸 코드 버그로, UI 복잡도로, 몬테카를로 수치 급변으로,
인버스 ETF 역효과로 — 서로 다른 층위에서 반복 확인했습니다.

그 결과 이 모델이 최종적으로 채택한 원칙은 다음과 같습니다:

- **기본적분석(펀더멘털 지표)을 축으로 삼는다** — green_count(실질금리·달러인덱스)처럼,
  0단계의 구간별 견고성 검증을 통과한 지표만 핵심 신호로 씁니다.
- **기술적분석은 가장 단순하고 오래된 모멘텀 이론 하나만 최소한으로 더한다** — 52주
  신고가/신저가 갱신처럼, 추가 파라미터가 거의 없는 원형에 가까운 장치만 남깁니다.
- **그 외 장치(노이즈 필터, 수수료 반영 등)는 기본값에서 배제한다** — 각 장치가 실제로
  유의미한 개선을 만드는지 스스로 A/B 검증하기 전까지는, 있으면 좋아 보이지만 검증되지
  않은 정교함으로 취급해 기본으로 켜두지 않습니다.
